#include<stdio.h>
#define max(a,b) ((a)>(b)?(a):(b))
int dp[12885][3];

int main(){
	int N, M, W, D;
	scanf("%d %d", &N, &M);
	for (int i = 0; i < N; i++)
	{
		scanf("%d %d", &W, &D);
		for (int j = 0; j <= M; j++)
		{
			
	}
}