#include <stdio.h>
#define MAX 1000

int N, A;
int backtracking[MAX];
int DP[MAX];

int main() {
    scanf("%d", &N);
    for (int i = 0; i < N; i++) {
        scanf("%d", &A);
        for (int j = i - 1; j >= 0; j--) {
        }
    }
}